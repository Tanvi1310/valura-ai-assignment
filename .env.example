# Copy to .env — never commit secrets
OPENAI_API_KEY=
# Development default; evaluators may set gpt-4.1
OPENAI_MODEL=gpt-4o-mini
# Optional base URL for Azure/OpenAI-compatible proxies
OPENAI_BASE_URL=
